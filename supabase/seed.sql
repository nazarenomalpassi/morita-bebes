-- Development data is intentionally empty.
-- Create login-capable users through local Studio, then create an organization
-- through the application so RLS and onboarding triggers are exercised.
select 1;
